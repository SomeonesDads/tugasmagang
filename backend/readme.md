The engineer directory endpoint is `GET /api/engineers` and returns engineer
Telegram IDs together with their assigned districts. The bot filters this
directory by `ENGINEER_DISTRICT` before sending notifications.
