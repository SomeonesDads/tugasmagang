The engineer directory endpoint is `GET /api/engineers` and returns engineer
Telegram IDs together with their assigned districts. The bot sends each
engineer the real ticket queue for that assignment.
